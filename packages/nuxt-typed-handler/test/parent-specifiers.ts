/**
 * The parents' `/types` specifiers the umbrella must make resolvable, restated
 * here rather than read off the source so the suites stay an independent
 * oracle for what the build writes.
 */
export const PARENT_SPECIFIERS = [
  '@dphonys/nuxt-handler-errors/types',
  '@dphonys/nuxt-handler-validation/types',
]
